import * as importTransaction from './transaction'

/** Generic element import (clone + reference/type closure) into a target document. */
export const importExtension = {
	transaction: importTransaction,
}
