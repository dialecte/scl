// Public types for the lifecycle `report` domain — the read-only classify surface a
// consumer (e.g. a merge-review UI) types against when calling `query.lifecycle.report`.

export { presentationScope } from './presentation-scope'
export type { PresentationScope } from './presentation-scope.types'
