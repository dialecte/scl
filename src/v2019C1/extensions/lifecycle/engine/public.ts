// Public types for the lifecycle `engine` domain — the diff/decision contract the
// report/apply surface is built on.

export type {
	AttributeChange,
	DecisionGroup,
	DecisionMap,
	DiffChange,
	DiffNode,
	DiffReport,
	DiffSummary,
	GroupConflict,
	GroupDecision,
} from './diff.types'
