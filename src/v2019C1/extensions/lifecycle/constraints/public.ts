// Public types for the lifecycle `constraints` domain — attribute editability the
// report surface tags each decision group with.

export type { AttributeEditability, EditableAttribute } from './classify-attribute.types'
