// Public presentation types: the `extractElementTitle` call contract plus the
// `TitleSpec`/`ConditionalTitle` authoring format for title overrides.

export type {
	ElementTitle,
	ExtractElementTitleOptions,
	ExtractElementTitleOptionsWithLabels,
} from './query/extract-element-title.types'
export type { ConditionalTitle, TitleSpec } from './constants/title.types'
